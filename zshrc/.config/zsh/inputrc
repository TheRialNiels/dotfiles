# Enable UTF-8 / meta handling
setopt MULTIBYTE
setopt PRINT_EIGHT_BIT

# # Make completion case-insensitive (like completion-ignore-case on)
# zstyle ':completion:*' matcher-list 'm:{a-zA-Z}={A-Za-z}'
#
# # Show menu automatically when multiple completions exist
# # setopt AUTO_MENU
#
# # Show completion even if unambiguous (like show-all-if-ambiguous)
# # setopt MENU_COMPLETE
#
# # Show completion list instead of auto-completing
# setopt AUTO_LIST
# setopt NO_MENU_COMPLETE
#
# # Show completion list first, then allow menu selection
# zstyle ':completion:*' menu select
#
# # Show all matches without paging (page-completions off)
# zstyle ':completion:*' list-prompt '%S%M matches:%s'
#
# # Allow completion to consider text before and after cursor
# setopt COMPLETE_IN_WORD
#
# # Do not match hidden files unless user types a dot (match-hidden-files off)
# zstyle ':completion:*' file-patterns \
#   '*(-/):directories' \
#   '.*(-/):hidden' \
#   '*(^-/):regular-files'
#
# # Mark symlinked directories with a trailing slash
# zstyle ':completion:*' special-dirs true
#
# # Show additional file info (like visible-stats)
# zstyle ':completion:*' list-colors ${(s.:.)LS_COLORS}
#
# # Limit completion query items (completion-query-items 200)
# zstyle ':completion:*' max-errors 200 numeric
#
# # Colored stats (similar to colored-stats on)
# zstyle ':completion:*' list-colors ${(s.:.)LS_COLORS}
#
